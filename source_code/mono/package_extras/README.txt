TOURNAMENT BUILDER COMMAND LINE INTERFACE
Pre-alpha version.

This provides the Tournament Builder as a command line interface (CLI).

It consists of an executable. Run the command:

> tournament_builder --help

to see command line options.

For full documentation and latest versions visit: https://github.com/arkadye/tournament_builder